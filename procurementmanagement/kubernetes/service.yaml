apiVersion: v1
kind: Service
metadata:
  name: procurementmanagement
  namespace: procurement
  labels:
    app: procurementmanagement
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: procurementmanagement